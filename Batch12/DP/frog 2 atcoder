int solve(int n ,int k)
{
    if(idx==n-1) return 0;
    if(dp[idx]!=-1) return dp[idx];
    
    
    for(int i=1;i<=k;i++)
    {
        if(idx+i>=n) break;
        
        ans=min(ans,solve(idx+1)+abs(arr[idx+i])=ar[idx]);
        
        return dp[idx]=ans;
    }
}
