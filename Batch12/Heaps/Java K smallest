class Solution {
    public int findKthLargest(int[] nums, int k) {
        PriorityQueue<Intege> pq=new PriorityQueue<>(nums);
        
        while(--k>0)
        {
            pq.pop();
        }
    }
    return pq.top();
}

