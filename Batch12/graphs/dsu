int parent [n];

for(int i=0i<n;i++)
parent[i]=i;

Node find Parent (Node vertex)
{
if(parent[vertex]==vertex)
return vertex;

return findParent(parent[vertex]);
}
void union(node u,node v)
{
Node parentv1=find Parent(v1);
Node parentv2=find Parent(v2);
if(parentv1 !=parentv2)
parent[v2]=v1;
}
