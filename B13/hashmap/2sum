//python code

for i in range(len(a)):
  target=k-a[i]
  if(target in ht):
    print(ht[target],i)
    break;
  ht[a[i]]=i
  
  
  //c++
  map<int,int> mp;
  for(int i=0;i<n;i++)
  {
  int b=nums[i];
  int a=target-b;
  if(mp.count(a))
  ptint(mp.count(a),i);
  mp[b]=i;
  
  
