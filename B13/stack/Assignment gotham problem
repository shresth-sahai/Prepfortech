Stack s;

res=[0 for i in range (n)]

for i in range(n-1,-1,-1):
  if (len(s)!=0):
    while(len(s)!=0 and s[-1] <=ar[i])
    s.pop()
    
    res[i]=-1 if len(s)==0 else s[-1]
    s.append(a[i])
    
    
    
    // java//cpp
    stack <int> s;
    int res[n];
    for(int i=0;i <n;i++)
    {
    if(!s.empty())
    {
    while(!s.empty() && s.top() <=arr[i]). s.pop();
    
    res[i]=s.empty()?-1:s.top();
    
    s.push(ar[i];
    }
    
